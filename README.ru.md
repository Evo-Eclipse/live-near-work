# Live Near Work

Мод для Surviving Mars, который автоматически переселяет колонистов ближе к их месту работы.

## Обзор

- *Переселение между куполами* — колонист работает в Куполе Б, но живет в Куполе А? Переместит в Б
- *Оптимизация внутри купола* — переселяет колонистов ближе к месту работы в пределах одного купола
- *Обработка перекрестных переселений* — корректно обрабатывает одновременные переезды A: X→Y и B: Y→X
- *Учет комфорта* — не будет переселять в значительно худшее жилье
- *Умная фильтрация* — учитывает детские, туристические и эксклюзивные резиденции
- *Проверка связности куполов* — переселяет только между соединенными куполами (проходы/шаттлы)
- *Автоматический запуск* — выполняется при смене смен (6:00, 14:00, 22:00)

## Установка

1. Скопируйте папку `live-near-work` в `%AppData%\Surviving Mars Relaunched\Mods\`
2. Активируйте в Менеджере модов
3. Запустите или загрузите игру

## Использование

### Консольные команды

```other
LiveNearWork.Run()          -- ручной запуск
LiveNearWork.Enable()       -- включить
LiveNearWork.Disable()      -- выключить
LiveNearWork.DebugEnable()  -- включить отладочные логи
LiveNearWork.DebugDisable() -- выключить отладочные логи
```

### Структура логов

```lua
Status  Colonist                  Dome Transfer               Residence Transfer                Reason
------  ------------------------  --------------------------  --------------------------------  --------------------
SKIP    Hope Bruno [-]            Lange #1 -> Buisson #1      Apartments -> ???                 no_space
SKIP    Bang Kwok [Sec]           Lange #1 -> Buisson #1      Apartments -> ???                 plan_full
OK      Larissa Schultz [Sci]     Lange #1 -> Buisson #1      Apartments -> Living Complex      workplace
OK      Zhuang Wei [Sci]          Buisson #1 -> Lange #1      Living Complex -> Apartments      workplace

[LNW] Done: 26 relocated (26 inter-dome, 0 intra-dome)
```

### Коды статуса

- `OK` — успешно переселен
- `SKIP` — не удалось переселить (см. Причину)

### Коды причин

- `workplace` — переселен в купол с местом работы (между куполами)
- `distance` — переселен ближе к месту работы (внутри купола)
- `no_space` — нет свободных мест в целевом куполе

## Настройки

Редактируйте в `Code/Script.lua` → `LiveNearWork.Settings`:

```lua
LiveNearWork = {
    Settings = {
        enabled = true,               -- Включить/выключить мод
        debug = false,                -- Отладочное логирование
        -- Триггеры
        trigger_hours = {6, 14, 22}, -- Часы запуска переселения
        scan_delay = 500,             -- Задержка после начала смены (мс)
        -- Правила
        allow_intra_dome = true,      -- Включить оптимизацию внутри купола
        max_comfort_loss = -10,       -- Макс. допустимое падение комфорта
        min_intra_score = 10,         -- Мин. улучшение для внутри-купола
        -- Оценка
        dist_score_max = 50,          -- Макс. бонус за близость (очки)
        dist_score_step = 1000        -- Шкала расстояния (игровые единицы)
    }
}
```

| Настройка        | По умолчанию | Описание                                                     |
| ---------------- | ------------ | ------------------------------------------------------------ |
| enabled          | true         | Главный переключатель мода                                   |
| debug            | false        | Включить подробные логи в консоли                            |
| trigger_hours    | {6, 14, 22}  | Игровые часы запуска переселения                             |
| scan_delay       | 500          | Миллисекунды ожидания после начала смены                     |
| allow_intra_dome | true         | Переселять колонистов внутри одного купола                   |
| max_comfort_loss | \-10         | Не переселять, если комфорт падает >10                       |
| min_intra_score  | 10           | Переселения внутри купола требуют +10 очков улучшения        |
| dist_score_max   | 50           | Макс. бонусные очки за близость                              |
| dist_score_step  | 1000         | Делитель расстояния (меньше = чувствительнее)                |

## Технические детали

### Как работает мод

#### Оценка

Каждая резиденция получает оценку на основе:

1. *Базовый комфорт* — значение комфорта обслуживания резиденции
2. *Бонус за близость* — ближе к месту работы = выше оценка (0-50 очков)
3. *Штраф за комфорт* — -1000 очков, если комфорт падает слишком сильно

Побеждает резиденция с лучшей оценкой!

#### Переселение

1. *Фильтрация* — Пропустить туристов, умирающих, транспортируемых, назначенных вручную колонистов
2. *Сбор* — Найти колонистов, чей купол работы ≠ купол резиденции
3. *Сортировка* — Сначала переселения между куполами (приоритет 2), затем внутри купола (приоритет 1)
4. *Планирование* — Зарезервировать места, обработать перекрестные переселения
5. *Выполнение* — Применить переселения, если место все еще доступно

### Исключения

Мод автоматически пропускает:

- *Туристов* — они используют собственную логику отелей
- *Резиденции с ручным назначением* — уважает ручные назначения
- *Детей* — переселяются только в/из Детских садов
- *Колонистов в транспорте* — чтобы избежать проблем с игровым движком
- *Умирающих/покидающих колонистов* — слишком поздно
- *Несвязанные купола* — нет доступного прохода или шаттла

### Примечания по совместимости

- ✅ Работает со всеми типами резиденций (Living Complex, Apartments, Arcology и т.д.)
- ✅ Учитывает здания только для детей (Nursery, School)
- ✅ Учитывает эксклюзивные резиденции (Senior Residence, Hotels)
- ✅ Совместим с модами проходов/шаттлов
- ⚠️ Может конфликтовать с другими модами автоназначения
- ⚠️ Может конфликтовать с модифицированными отелями или детскими садами

## История версий

### Версия 1.0

- Первый релиз
- Переселение между куполами и внутри купола
- Выбор резиденции с учетом комфорта
- Автоматическое выполнение при смене смен

---

Created with 💜 and Lua by Evo-Eclipse
